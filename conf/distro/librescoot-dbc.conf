include conf/distro/include/fsl-imx-base.inc
include conf/distro/include/fsl-imx-preferred-env.inc

DISTRO = "librescoot-dbc"

DISTRO_NAME = "LibreScoot"
DISTRO_CODENAME ?= "none"

DISTRO_FEATURES:remove = "directfb zeroconf avahi 3g nfc bluetooth pcmcia wifi pci"
DISTRO_FEATURES:append = " wayland pam systemd usrmerge"

DISTRO_VERSION := "${LIBRESCOOT_VERSION}"

# Set default timezone to Europe/Berlin
DEFAULT_TIMEZONE = "Europe/Berlin"

