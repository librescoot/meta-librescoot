# imx_v6_v7_defconfig sets EXPERT=y, which flips the NAMESPACES default
# to n. Without namespaces, systemd sandboxing directives (ProtectHostname,
# PrivateTmp, ProtectSystem, ...) are silently skipped at runtime.
CONFIG_NAMESPACES=y
CONFIG_UTS_NS=y
CONFIG_IPC_NS=y
CONFIG_PID_NS=y
CONFIG_NET_NS=y
