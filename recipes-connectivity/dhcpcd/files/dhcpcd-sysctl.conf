# Increase BPF ancillary buffer space for dhcpcd
# dhcpcd 10.x removed the bpf_buflen config option;
# the kernel's optmem_max now controls this.
# Default 10240 is too small, causing "No buffer space available" errors.
net.core.optmem_max = 65536
