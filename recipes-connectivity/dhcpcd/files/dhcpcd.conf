# Deny DHCP on CAN interfaces
# CAN (Controller Area Network) is for vehicle communications, not IP networking
denyinterfaces can*

# Increase BPF buffer size to prevent packet drops
# Default is often too small for busy networks
bpf_buflen 65536
