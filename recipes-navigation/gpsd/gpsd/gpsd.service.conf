# Ensure gpsd starts after the system clock has been seeded by
# fake-hwclock.  gpsd uses the system time to resolve GPS week-number
# rollover in NMEA dates; without a sane clock it may output dates
# ~1024 weeks in the past.
[Unit]
After=fake-hwclock-load.service
