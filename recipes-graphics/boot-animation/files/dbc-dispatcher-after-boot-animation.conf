[Unit]
# Flutter renders to fb1 (overlay, invisible) while boot-animation runs on fb0.
# The overlay-crossfade service handles the transition.
# No ordering dependency needed - they run in parallel.
Wants=overlay-crossfade.service
