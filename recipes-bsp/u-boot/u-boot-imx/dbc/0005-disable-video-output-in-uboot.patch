From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LibreScoot <dev@librescoot.org>
Date: Mon, 01 Jan 2025 00:00:00 +0000
Subject: [PATCH] disable video output in U-Boot

Disable CONFIG_VIDEO so U-Boot does not initialise the IPU framebuffer
or render a logo. The LCD panel is still initialised via SPI in
board_init() → enable_rgb(), which is not guarded by CONFIG_VIDEO_IPUV3.

Without U-Boot touching the framebuffer the kernel DRM driver starts
with a clean state. The pwm-backlight driver holds the backlight off
until it loads and sets the brightness, so the display remains dark
until the kernel boot logo is ready.
---
 configs/mx6solosabresd_defconfig | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/configs/mx6solosabresd_defconfig b/configs/mx6solosabresd_defconfig
index 5a6de4eb9de..0000000000a 100644
--- a/configs/mx6solosabresd_defconfig
+++ b/configs/mx6solosabresd_defconfig
@@ -3,7 +3,7 @@ CONFIG_ARCH_MX6=y
 CONFIG_TARGET_MX6SABRESD=y
-CONFIG_VIDEO=y
+# CONFIG_VIDEO is not set
 CONFIG_SYS_EXTRA_OPTIONS="IMX_CONFIG=board/freescale/mx6sabresd/mx6solo_4x_mt41j128.cfg,MX6S"
 CONFIG_BOOTDELAY=0
 # CONFIG_CONSOLE_MUX is not set
 CONFIG_SYS_CONSOLE_IS_IN_ENV=y
 CONFIG_SYS_CONSOLE_OVERWRITE_ROUTINE=y
--
2.34.1
