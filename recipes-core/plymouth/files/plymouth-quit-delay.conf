# Delay plymouth quit to allow boot animation to complete before handoff.
# The librescoot animation is 8s + 1s fade; without this delay, multi-user.target
# fires before the animation finishes.
[Service]
ExecStartPre=/bin/sleep 3
